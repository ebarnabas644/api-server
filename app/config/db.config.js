const  config = {
    USER:  'root', // sql user
    PASSWORD:  'root', //sql user password
    HOST:  '127.0.0.1', // if it does not work try- localhost
    DB:  'gameSite',
    PORT:  3306
  }
  
  module.exports = config;
